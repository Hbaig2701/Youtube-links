{
  "installCommand": "npm install",
  "buildCommand": "./node_modules/.bin/vite build --root client",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/go/(.*)", "destination": "/api" }
  ]
}
